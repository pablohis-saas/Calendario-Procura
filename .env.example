# Ejemplo de configuración para backend (cobros)
DATABASE_URL=file:./backend/prisma/dev.db
JWT_SECRET=supersecreto123
NODE_ENV=development
PORT=3002
# Twilio (opcional, solo si usas recordatorios por WhatsApp)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=

# Ejemplo de configuración para inventory-module (inventario)
# (Copia este bloque a inventory-module/.env si lo usas por separado)
# DATABASE_URL para PostgreSQL/Supabase
DATABASE_URL=postgresql://usuario:contraseña@host:puerto/base_datos
JWT_SECRET=tu_jwt_secret_super_seguro
NODE_ENV=development
NEXT_PUBLIC_API_URL=http://localhost:3000